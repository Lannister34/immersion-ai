import { PlaceholderScreen } from '../../shared/ui/placeholder-screen';

export function ScenariosScreen() {
  return (
    <PlaceholderScreen
      eyebrow="сценарии"
      title="Сценарии"
      description="Сценарии остаются отдельным owner-aligned frontend module, а не частью character или chat state."
    />
  );
}
